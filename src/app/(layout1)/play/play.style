@scope {
  .section {
    min-width: 100vw;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 50rem 0 50rem 0;
  }

  .play-cards {
    grid-column: span 4 / 9;
    grid-row: 1;
    padding-top: 5rem;
  }
}
