@scope {
  .section {
    min-width: 100vw;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 30rem 0 30rem 0;
  }

  :not(h1).title {
    translate: 0 -60px;
  }

  .play-cards {
    grid-column: span 4 / 9;
    grid-row: 1;
    padding-top: 5rem;
  }
}
